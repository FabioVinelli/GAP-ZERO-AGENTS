# Copy to .env and fill in. The key never reaches the browser — the Express proxy holds it.
ANTHROPIC_API_KEY=sk-ant-...
# Any Anthropic model string works (model-agnostic engine):
MODEL=claude-sonnet-4-6
PORT=8787
